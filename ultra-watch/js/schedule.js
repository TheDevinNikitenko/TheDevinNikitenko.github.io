const schedule = {
  "2026-03-27": {
    "Mainstage": [
      { name: "Frank Walker", start: "16:00", end: "16:50" },
      { name: "Voyd", start: "16:50", end: "17:55" },
      { name: "Worship", start: "17:55", end: "19:00" },
      { name: "Illenium", start: "19:00", end: "20:05" },
      { name: "Bzrp", start: "20:05", end: "21:20" },
      { name: "Alesso B2B Martin Garrix", start: "21:20", end: "22:45" },
      { name: "Major Lazer", start: "22:45", end: "23:59" }
    ],
    "Worldwide": [
      { name: "PRADA2000", start: "16:00", end: "17:00" },
      { name: "MATTY RALPH", start: "17:00", end: "18:00" },
      { name: "SUPERSTRINGS", start: "18:00", end: "19:00" },
      { name: "VINI VICI", start: "19:00", end: "20:00" },
      { name: "LILLY PALMER", start: "20:00", end: "21:00" },
      { name: "ARMIN VAN BUUREN B2B MARLON HOFFSTADT", start: "21:00", end: "22:30" },
      { name: "ARMIN VAN BUUREN", start: "22:30", end: "23:59" }
    ],
    "Megastructure": [
      { name: "MAR-T", start: "16:00", end: "17:30" },
      { name: "MASSANO", start: "17:30", end: "19:00" },
      { name: "MISS MONIQUE", start: "19:00", end: "20:30" },
      { name: "VINTAGE CULTURE", start: "20:30", end: "22:00" },
      { name: "ERIC PRYDZ", start: "22:00", end: "23:59" }
    ],
    "Cove": [
      { name: "GODTRIPPER", start: "16:00", end: "17:30" },
      { name: "ALT8", start: "17:30", end: "19:00" },
      { name: "999999999", start: "19:00", end: "20:30" },
      { name: "CLARA CUVÉ", start: "20:30", end: "22:00" },
      { name: "SARA LANDRY", start: "22:00", end: "23:59" }
    ],
    "Live Stage": [
        { name: "Afrobeta", start: "17:00", end: "18:15" },
        { name: "Shima", start: "18:15", end: "19:45" },
        { name: "Black Tigver Sex Machine presents Connected Fighters", start: "19:45", end: "21:15" },
        { name: "Of The Trees", start: "21:15", end: "22:45" },
        { name: "Levity presents Lasership", start: "22:45", end: "23:59" },
    ]
  },

  "2026-03-28": {
    "Mainstage": [
      { name: "Gil Glaze", start: "12:00", end: "12:55" },
      { name: "Myrkris", start: "12:55", end: "14:00" },
      { name: "Halo", start: "14:00", end: "15:05" },
      { name: "Nicky Romero", start: "15:05", end: "16:10" },
      { name: "Loud Luxury", start: "16:10", end: "17:15" },
      { name: "Alan Walker", start: "17:15", end: "18:20" },
      { name: "Steve Aoki", start: "18:20", end: "19:25" },
      { name: "Excision", start: "19:25", end: "20:30" },
      { name: "Hardwell", start: "20:30", end: "21:35" },
      { name: "Armin Van Buuren", start: "21:35", end: "22:45" },
      { name: "Sebastian Ingrosso B2B Steve Angello", start: "22:45", end: "23:59" }
    ],
    "Worldwide": [
      { name: "JULIAN CROSS", start: "12:10", end: "13:00" },
      { name: "DISTINCT MOTIVE", start: "13:00", end: "14:00" },
      { name: "SIDEQUEST", start: "14:00", end: "15:00" },
      { name: "DANIEL ALLAN", start: "15:00", end: "16:00" },
      { name: "ARMNHMR", start: "16:00", end: "17:00" },
      { name: "HOL!", start: "17:00", end: "18:00" },
      { name: "BOU", start: "18:00", end: "19:00" },
      { name: "KAI WACHI", start: "19:00", end: "20:00" },
      { name: "OUTLAW B2B TRYM", start: "20:00", end: "21:00" },
      { name: "HAMDI", start: "21:00", end: "22:00" },
      { name: "YOSUKE YUKIMATSU", start: "22:00", end: "23:00" },
      { name: "ISOXO", start: "23:00", end: "23:59" }
    ],
    "Megastructure": [
      { name: "ANDY PATE B2B ROD B.", start: "12:00", end: "13:15" },
      { name: "JULIET FOX", start: "13:15", end: "14:30" },
      { name: "ALAN FITZPATRICK B2B MARCO FARAONE", start: "14:30", end: "16:00" },
      { name: "SASHA B2B JOHN DIGWEED", start: "16:00", end: "17:30" },
      { name: "ELI BROWN", start: "17:30", end: "19:00" },
      { name: "ADAM BEYER B2B JOSEPH CAPRIATI", start: "19:00", end: "21:00" },
      { name: "CARL COX", start: "21:00", end: "23:59" }
    ],
    "Cove": [
      { name: "JOA", start: "12:00", end: "13:15" },
      { name: "OLYMPE", start: "13:15", end: "14:30" },
      { name: "RIVO", start: "14:30", end: "16:00" },
      { name: "KASIA", start: "16:00", end: "17:30" },
      { name: "DEEP DISH", start: "17:30", end: "19:00" },
      { name: "COLYN B2B INNELLEA", start: "19:00", end: "20:30" },
      { name: "CASSIAN", start: "20:30", end: "22:00" },
      { name: "JORIS VOORN B2B KOROLOVA", start: "22:00", end: "23:59" }
    ],
    "Live Stage": [
        { name: "Miss Bashful", start: "16:00", end: "17:10" },
        { name: "Confidence Man", start: "17:10", end: "18:30" },
        { name: "Snow Strippers", start: "18:30", end: "20:00" },
        { name: "Madeon", start: "20:00", end: "21:30" },
        { name: "Boys Noize Live", start: "21:30", end: "22:45" },
        { name: "Brutalismus 3000", start: "22:45", end: "23:59" },
    ]
  },

  "2026-03-29": {
    "Mainstage": [
      { name: "Kapuchon", start: "12:15", end: "13:05" },
      { name: "Me N U", start: "13:05", end: "14:10" },
      { name: "R3hab", start: "14:10", end: "15:15" },
      { name: "Maddix", start: "15:15", end: "16:20" },
      { name: "Artbat", start: "16:20", end: "17:25" },
      { name: "Marlon Hoffstadt", start: "17:25", end: "18:30" },
      { name: "Afrojack", start: "18:30", end: "19:35" },
      { name: "DJ Snake", start: "19:35", end: "20:45" },
      { name: "John Summit", start: "20:45", end: "22:00" }
    ],
    "Worldwide": [
      { name: "BIG FLORIDA", start: "12:00", end: "13:00" },
      { name: "JSTJR", start: "13:00", end: "14:00" },
      { name: "BOLO", start: "14:00", end: "15:00" },
      { name: "LAYZ", start: "15:00", end: "16:00" },
      { name: "ANDY C", start: "16:00", end: "17:00" },
      { name: "HOLY PRIEST", start: "17:00", end: "18:00" },
      { name: "PEEKABOO", start: "18:00", end: "19:00" },
      { name: "RAY VOLPE B2B SULLIVAN KING", start: "19:00", end: "20:30" },
      { name: "WANKDAT", start: "20:30", end: "22:00" }
    ],
    "Megastructure": [
      { name: "BASSETT B2B CHRISTOPHER JAMES", start: "12:00", end: "14:00" },
      { name: "SCENARIOS", start: "14:00", end: "15:30" },
      { name: "ARGY B2B MIND AGAINST", start: "15:30", end: "17:00" },
      { name: "ADRIATIQUE", start: "17:00", end: "18:30" },
      { name: "BORIS BREJCHA", start: "18:30", end: "20:00" },
      { name: "AMELIE LENS B2B SARA LANDRY", start: "20:00", end: "22:00" }
    ],
    "Cove": [
      { name: "M.O.N.R.O.E.", start: "12:00", end: "13:00" },
      { name: "PLASTIK FUNK", start: "13:00", end: "14:00" },
      { name: "BLACK FANCY", start: "14:00", end: "15:15" },
      { name: "MALONE", start: "15:15", end: "16:30" },
      { name: "ROSSI.", start: "16:30", end: "18:00" },
      { name: "DENNIS CRUZ B2B SETH TROXLER", start: "18:00", end: "20:00" },
      { name: "THE MARTINEZ BROTHERS", start: "20:00", end: "22:00" }
    ],
    "Live Stage": [
        { name: "Boiish", start: "16:00", end: "17:00" },
        { name: "Parisi", start: "17:00", end: "18:15" },
        { name: "Louis The Child", start: "18:15", end: "19:30" },
        { name: "Tim3less", start: "19:30", end: "21:00" },
        { name: "Zhu", start: "21:00", end: "22:00" },
    ]
  }
};

// =========================
// GLOBAL STATE
// =========================
let activeDayKey = null;
let fallbackCountdownInterval = null;
const countdownIntervals = {};

// =========================
// DATE HELPERS
// =========================
function getESTDateString(date = new Date()) {
    return new Date(date.toLocaleString("en-US", { timeZone: "America/New_York" }))
        .toISOString()
        .split("T")[0];
}

function getFirstScheduleDay() {
    return Object.keys(schedule).sort()[0];
}

function setActiveDay() {
    const todayEST = getESTDateString();

    if (schedule[todayEST]) {
        activeDayKey = todayEST;
    } else {
        activeDayKey = getFirstScheduleDay();
        startFallbackCountdown();
    }
}

// =========================
// CLOCK (TOP BAR)
// =========================
function updateTime() {
    const now = new Date();
    const est = new Date(now.toLocaleString("en-US", { timeZone: "America/New_York" }));

    const timeStr = est.toLocaleTimeString('en-US', {
        hour: '2-digit',
        minute: '2-digit',
        hour12: false
    });

    const dateStr = est.toLocaleDateString('en-US', {
        weekday: 'short',
        month: 'short',
        day: 'numeric'
    });

    const el = document.getElementById('current-time');
    if (el) {
        el.textContent = `EST ${timeStr} · ${dateStr}`;
    }
}

// =========================
// FALLBACK COUNTDOWN (BEFORE FESTIVAL)
// =========================
function startFallbackCountdown() {
    const el = document.getElementById("current-time");
    if (!el) return;

    if (fallbackCountdownInterval) {
        clearInterval(fallbackCountdownInterval);
    }

    fallbackCountdownInterval = setInterval(() => {
        const now = new Date();
        const estNow = new Date(now.toLocaleString("en-US", { timeZone: "America/New_York" }));

        const start = new Date(activeDayKey + "T16:00:00");
        const diff = start - estNow;

        if (diff <= 0) {
            clearInterval(fallbackCountdownInterval);
            setActiveDay();
            renderSchedule();
            return;
        }

        const h = Math.floor(diff / (1000 * 60 * 60));
        const m = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
        const s = Math.floor((diff % (1000 * 60)) / 1000);

        el.textContent = `Festival starts in ${h}h ${m}m ${s}s`;
    }, 1000);
}

// =========================
// COUNTDOWN TIMER
// =========================
function startCountdown(artistEl, endTime) {
    if (countdownIntervals[artistEl.id]) {
        clearInterval(countdownIntervals[artistEl.id]);
    }

    function update() {
        const now = new Date();
        const estNow = new Date(now.toLocaleString("en-US", { timeZone: "America/New_York" }));

        const [h, m] = endTime.split(":").map(Number);
        const end = new Date(estNow);
        end.setHours(h, m, 0, 0);

        if (end < estNow) end.setDate(end.getDate() + 1);

        const diff = end - estNow;

        const el = artistEl.querySelector(".countdown");

        if (diff <= 0) {
            if (el) el.textContent = "ENDING";
            checkForSetChanges();
            return;
        }

        const hh = Math.floor(diff / 3600000);
        const mm = Math.floor((diff % 3600000) / 60000);
        const ss = Math.floor((diff % 60000) / 1000);

        if (el) {
            el.textContent = `${hh.toString().padStart(2,'0')}:${mm.toString().padStart(2,'0')}:${ss.toString().padStart(2,'0')}`;
        }
    }

    update();
    countdownIntervals[artistEl.id] = setInterval(update, 1000);
}

// =========================
// SET CHANGE DETECTION
// =========================
function checkForSetChanges() {
    const now = new Date();
    const est = new Date(now.toLocaleString("en-US", { timeZone: "America/New_York" }));
    const currentTime = est.getHours() * 60 + est.getMinutes();

    const daySchedule = schedule[activeDayKey];

    document.querySelectorAll('.stage').forEach(stageEl => {
        const stageName = stageEl.querySelector('.stage-name').textContent;
        const artists = daySchedule[stageName];
        const artistEls = stageEl.querySelectorAll('.artist');

        artists.forEach((artist, i) => {
            const [sh, sm] = artist.start.split(":").map(Number);
            const [eh, em] = artist.end.split(":").map(Number);

            const start = sh * 60 + sm;
            const end = eh * 60 + em;

            const el = artistEls[i];

            if (currentTime >= start && currentTime < end) {
                if (!el.classList.contains("current-artist")) {
                    const prev = stageEl.querySelector(".current-artist");

                    if (prev) {
                        prev.classList.remove("current-artist");
                        prev.querySelector(".countdown")?.remove();
                    }

                    el.classList.add("current-artist");

                    const c = document.createElement("div");
                    c.className = "countdown";
                    el.appendChild(c);

                    startCountdown(el, artist.end);
                }
            }
        });
    });
}

// =========================
// RENDER
// =========================
function renderSchedule() {
    const container = document.getElementById("stages-container");
    if (!container) return;

    Object.values(countdownIntervals).forEach(clearInterval);
    container.innerHTML = "";

    const daySchedule = schedule[activeDayKey];

    // ✅ FIX: proper label targeting (getElementsByClassName returns HTMLCollection)
    const label = document.querySelector(".stage-tracker-title");
    if (label) {
        const dateObj = new Date(activeDayKey + "T00:00:00");
        const dayName = dateObj.toLocaleDateString("en-US", { weekday: "long" });
        label.innerHTML = `ULTRA 2026 • ${dayName} SCHEDULE`;
    }

    const now = new Date();
    const est = new Date(now.toLocaleString("en-US", { timeZone: "America/New_York" }));
    const currentTime = est.getHours() * 60 + est.getMinutes();

    for (const [stageName, artists] of Object.entries(daySchedule)) {
        const stageEl = document.createElement("div");
        stageEl.className = "stage";

        // ✅ RESTORED: stage-header wrapper
        const stageHeader = document.createElement("div");
        stageHeader.className = "stage-header";

        const header = document.createElement("h2");
        header.className = "stage-name";
        header.textContent = stageName;

        stageHeader.appendChild(header);
        stageEl.appendChild(stageHeader);

        // ✅ RESTORED: artist-list class
        const list = document.createElement("ul");
        list.className = "artist-list";

        artists.forEach((artist, i) => {
            const [sh, sm] = artist.start.split(":").map(Number);
            const [eh, em] = artist.end.split(":").map(Number);

            const start = sh * 60 + sm;
            const end = eh * 60 + em;

            const isCurrent = currentTime >= start && currentTime < end;

            const li = document.createElement("li");
            li.className = `artist ${isCurrent ? "current-artist" : ""}`;
            li.id = `${stageName.replace(/\s+/g, '-')}-${artist.name.replace(/\s+/g, '-')}`;

            // ✅ RESTORED: artist-info wrapper (important for layout)
            const artistInfo = document.createElement("div");
            artistInfo.className = "artist-info";

            const nameSpan = document.createElement("span");
            nameSpan.className = "artist-name";
            nameSpan.textContent = artist.name;

            const artistNameBR = document.createElement("br");

            const timeSpan = document.createElement("span");
            timeSpan.className = "artist-time";
            timeSpan.textContent = `${artist.start} - ${artist.end}`;

            artistInfo.appendChild(nameSpan);
            artistInfo.append(artistNameBR);
            artistInfo.appendChild(timeSpan);
            li.appendChild(artistInfo);

            // ✅ Countdown stays the same
            if (isCurrent) {
                const c = document.createElement("div");
                c.className = "countdown";
                li.appendChild(c);
                startCountdown(li, artist.end);
            }

            list.appendChild(li);
        });

        stageEl.appendChild(list);
        container.appendChild(stageEl);
    }
}

function renderDayTabs() {
    const tabsContainer = document.getElementById("day-tabs");
    if (!tabsContainer) return;

    tabsContainer.innerHTML = "";

    const days = Object.keys(schedule).sort();

    days.forEach(dayKey => {
        const dateObj = new Date(dayKey + "T00:00:00");
        const dayName = dateObj.toLocaleDateString("en-US", { weekday: "short" });

        const tab = document.createElement("button");
        tab.className = `day-tab ${dayKey === activeDayKey ? "active" : ""}`;
        tab.textContent = dayName;

        tab.onclick = () => {
            activeDayKey = dayKey;

            // stop fallback countdown if switching manually
            if (fallbackCountdownInterval) {
                clearInterval(fallbackCountdownInterval);
            }

            renderDayTabs();
            renderSchedule();
        };

        tabsContainer.appendChild(tab);
    });
}

// =========================
// INIT
// =========================
document.addEventListener("DOMContentLoaded", () => {
    setActiveDay();
    updateTime();
    renderDayTabs();   // 👈 ADD THIS
    renderSchedule();

    setInterval(updateTime, 1000);
    setInterval(checkForSetChanges, 1000);
});